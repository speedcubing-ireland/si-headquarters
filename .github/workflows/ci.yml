name: CI

on:
  pull_request:
    branches: [main]
  workflow_call:

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Lint
        run: bun run lint

      - name: Verify lint produced no changes
        run: git diff --exit-code

      - name: Lint convex
        run: bun run lint:convex

      - name: Verify lint:convex produced no changes
        run: git diff --exit-code

      - name: Type check
        run: bun run typecheck

      - name: Tests
        run: bun run test:once
